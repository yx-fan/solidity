pragma solidity >=0.7.0 < 0.9.0;

// Three main types of variables
// Boolean: true or false
// Integer: uint
// String: characters

contract learnVariables {
    // all our solidity code

    uint chocolateBar = 10;
    uint storeOwner = 300;
    bool liedetector = true;
    string errorMessageText = "Error! There has a been a mistake!";

    uint wallet = 500;
    bool spend = false;
    string notifySpend = "you have spent money";

}
